# Overview

## Commands

```sh
# Common
npx nx build ui
npx nx test ui
npx nx lint ui

# Run them together!
npx nx run-many -p ui -t build test lint

# View project details
npx nx show project ui

# View interactive project graph
npx nx graph

# View interactive ui
npx nx run ui:storybook
```

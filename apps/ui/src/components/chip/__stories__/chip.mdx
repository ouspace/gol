import { Canvas, Meta, Controls, ArgTypes } from '@storybook/addon-docs/blocks';
import { Playground } from 'storybook-addon-code-editor';

import * as Components from '../index';
import * as Stories from './chip.stories';
import DefaultSnippet from './default.snippet?raw';

<Meta of={Stories} />

# Chip

Chips are compact elements that represent an input, attribute, or action. They support four roles
— `assist`, `filter`, `input`, and `suggestion` — and can be rendered as a button or an anchor.

<Canvas of={Stories.Default} layout='centered' />

## Controls

<Controls of={Stories.Default} />

## API

<ArgTypes of={Stories.Default} />

## Playground

<Playground
	code={DefaultSnippet}
	availableImports={{ '@gol/ui': Components }}
	defaultEditorOptions={{
		minimap: { enabled: false },
		scrollBeyondLastLine: false,
		automaticLayout: false,
	}}
	modifyEditor={(monaco, editor) => {
		monaco.languages.typescript.typescriptDefaults.setDiagnosticsOptions({
			noSemanticValidation: true,
		});
	}}
/>

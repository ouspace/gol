import { SafeAreaView, ScrollView, View, Text } from 'react-native';

export default function Application() {
	return (
		<SafeAreaView>
			<ScrollView>
				<View>
					<Text>Hi...</Text>
				</View>
			</ScrollView>
		</SafeAreaView>
	)
}
